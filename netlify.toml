# Netlify Configuration
[build]
command = "pnpm build"
publish = "dist"
node_version = "20"

# Redirect rules for SPA (Single Page Application)
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
